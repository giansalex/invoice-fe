{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1><i class="fa fa-plus"></i> Nueva Serie</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
                <button class="btn btn-primary">Crear</button>
            </div>
        </div>
        {{ form_end(form) }}

        <a href="{{ path('series-documento_index') }}"><i class="fa fa-angle-double-left"></i> Regresar a lista</a>
    </div>
{% endblock %}
